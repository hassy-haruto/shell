#!/bin/bash
#if

read -p "Name? " name
# if [test] "$name" = "ohashi"
# if [ "$name" = "ohashi" ]
# then
#   echo "welcome"
# elif [ "$name" = "hashiguchi" ]
# then
#   echo "welcome too"
# else 
#   echo "you are not allowed"
# fi

if [ "$name" = "ohashi" ]; then
  echo "welcome"
elif [ "$name" = "hashiguchi" ]; then
  echo "welcome too"
else 
  echo "you are not allowed"
fi

# 実行コマンド
