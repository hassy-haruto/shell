#!/bin/bash
#if
AND は && 、 OR が || 、NOT は !
#&& || !

# 種類を問わず存在しているか調べるには-e
# ファイルが存在しているかは-f
# ディレクトリが存在しているかどうかは-d
# -e -f -d

# if [[ -f $0 ]]; then
#   echo "file exists..."
# fi

# if [[ -f $0 ]]; then
#   echo "dir exists..."
# fi

# == != > >= < <=
read -p "Number? " n
if ((n > 10));then
  echo "bigger than 10"
fi
